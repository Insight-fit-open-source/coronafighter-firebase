rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /screenings/{userId} {
      allow create, read, write, update: if request.auth.uid == userId;
    }
  }
}
